def bubble_sort(arr):
    n=len(arr)
    for i in range(n):
        for j in range(0,n-i-1):
            if arr[j]>arr[j+1]:
                arr[j],arr[j+1]=arr[j+1],arr[j]
    return arr
unsorted_lst=[64,35,25,12]
sorted_lst=bubble_sort(unsorted_lst)
print(sorted_lst)
